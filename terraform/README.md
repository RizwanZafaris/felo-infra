# Terraform

Skeleton — no resources defined yet. Owned by `devops`.

Planned modules:
- `modules/supabase` — project + RLS + storage
- `modules/vercel` — portal hosting
- `modules/fly` — backend hosting
- `modules/sentry` — error tracking
- `modules/cloudflare` — DNS + WAF
